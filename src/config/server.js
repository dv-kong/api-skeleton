class Server {
    #http; // private
    constructor(http) {
        this.#http = http;

}

// #http.use()
controller(controller) {}
middleware(middleware) {}
model(model){}


}

export default Server;